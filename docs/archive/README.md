# Archived documentation

These files are preserved for provenance from earlier deployment stages. They
contain the original implementation instruction, handover notes, pre-load
state, and first profiling summary. Some commands and filenames are
intentionally obsolete after the cleanup.

Use the current documents in the parent `docs/` directory for all operations.
